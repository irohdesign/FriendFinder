// GET route with url /api/friends that displays the JSON of all possible friends (objects)

// POST route to /api/friends that handles incoming survey results. route will also be used to handle compatibility logic